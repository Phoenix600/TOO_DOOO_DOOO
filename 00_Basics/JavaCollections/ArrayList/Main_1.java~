import java.lang.ProcessBuilder.Redirect.Type;
import java.util.*;

public class Main_1
{
	public static void main(String...args)
	{
		// Getting started with the Array List 
		List<Integer> list = new ArrayList<Integer>();

		//  
		list.add(12);
		System.out.printf("%d",list.get(0));

		Integer ARR[] = new Integer[]{13,14,18,19,17,};
		list.addAll(Arrays.asList(ARR));

		System.out.println("Elements in the Array List Are : "  + list.toString());


		// Conversion of primitive array into the Integer Array 
		int ArrayPrimitive[] = {14,15,16,17,18,19,21};
		System.out.println("Type : " + ArrayPrimitive.getClass());

		ArrayList<Integer> Arr = new ArrayList<Integer>();
		
		Integer[] bx = new Integer[ArrayPrimitive.length];

		for(int i=0; i<ArrayPrimitive.length; i++)
		{
			bx[i] = Integer.valueOf(ArrayPrimitive[i]);
		}

		System.out.println("Elements in the Integr array : " + (bx).toString());
		System.out.println(Arrays.toString(bx));

		

	}
}
